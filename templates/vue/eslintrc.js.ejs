module.exports = {
  root: true,
  env: {
    node: true
  },
  'extends': [
    'plugin:vue/vue3-essential',<% if (config.features.includes('typescript')) { %>
    '@typescript-eslint/recommended',<% } %>
    'eslint:recommended'
  ],
  parserOptions: {
    ecmaVersion: 2020<% if (config.features.includes('typescript')) { %>,
    parser: '@typescript-eslint/parser'<% } %>
  },
  rules: {
    'no-console': process.env.NODE_ENV === 'production' ? 'warn' : 'off',
    'no-debugger': process.env.NODE_ENV === 'production' ? 'warn' : 'off'
  }
}
